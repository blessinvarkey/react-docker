import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function Index() {
  return (
      <div> This is a text file</div>
  )
}

const styles = StyleSheet.create({})